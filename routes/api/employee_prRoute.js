const express = require("express");

const employee_prController = require("../../controllers/assessment/employee_prController");

const router = express.Router();

//router.post("/add", employee_prController.addEmployee_pr)

module.exports = router;